# Lab: Harden The Harness

Chinese: [lab.zh.md](lab.zh.md)

Add a context-size budget, a per-tool call limit, cancellation, and a redacted trace hook to `AgentHarness`. Tests must prove step exhaustion, tool exhaustion, timeout, unknown action, and successful termination. Pass at 80%; authorization bypass is an automatic failure.

