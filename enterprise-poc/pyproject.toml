[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xingai-enterprise-poc"
version = "0.1.0"
description = "Production-shaped enterprise AI reference POC"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
